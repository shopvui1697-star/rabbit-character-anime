{
  "name": "rabbit-ai-avatar",
  "version": "1.0.0",
  "private": true,
  "description": "AI Live Conversation Avatar with Rabbit character",
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install",
    "db:setup": "cd backend && npm run db:setup",
    "generate-waiting": "cd backend && npm run generate-waiting",
    "generate:short-waiting": "cd backend && npm run generate:short-waiting",
    "generate-all-waiting": "cd backend && npm run generate-all-waiting"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
